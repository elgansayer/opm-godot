# OpenMoHAA GDExtension Debugging: Next Steps

## Current Status
- **Success**: `libopenmohaa.so` now compiles and links successfully!
- **Fixed**: `code/corepp` added to build, resolving the `_ZTI5Class` (vtable for Class) missing symbol.
- **Fixed**: Compilation errors in `configurator.cpp` (missing `glbs` and syntax error in `Com_sprintf`).
- **Improved**: `openmohaa.gdextension` updated with mandatory `compatibility_minimum = "4.2"` and broadened platform keys.
- **New Blocker**: Godot fails to load the extension with the following error:
  > `ERROR: Can't open dynamic library: .../libopenmohaa.so. Error: .../libopenmohaa.so: undefined symbol: _ZNK11FallingRock9classinfoEv.`

## Immediate Tasks for Next Session

### 1. Resolve `FallingRock` Undefined Symbol
The symbol `_ZNK11FallingRock9classinfoEv` corresponds to `FallingRock::classinfo() const`. This is the metadata function generated by the `CLASS_PROTOTYPE` and `CLASS_DECLARATION` macros.
- **Location**: `FallingRock` is defined in `code/fgame/misc.h`.
- **Action**: Check `code/fgame/misc.cpp` to see if `CLASS_DECLARATION(Entity, FallingRock, ...)` exists.
- **Hypothesis**: Either the declaration is missing, commented out, or guarded by a `#ifdef` that is not active in the current build.

### 2. Verify Monolith Symbol Visibility
Since we are building a "Monolith" (including `server`, `fgame`, `qcommon`, etc., into one `.so`), we need to ensure all game entities are being linked correctly. 
- If `misc.cpp` is being compiled but the symbol is still missing, check if `FallingRock` methods are actually implemented.

### 3. Godot Runtime Loop Verification
Once the library loads successfully:
- Re-run the Godot headless test:
  ```bash
  /home/elgan/.local/bin/godot --headless --path /home/elgan/dev/opm-godot/project --verbose --quit-after 100
  ```
- Look for the debug prints:
  - `OpenMoHAA: openmohaa_library_init called.`
  - `OpenMoHAA: initialize_openmohaa_module called at SCENE level.`
  - `Main: MoHAARunner added to tree (dynamic).`

### 4. Headless Server Integration
Once `MoHAARunner` is alive in the tree:
- Implement the actual call to the OpenMoHAA server frame logic inside `MoHAARunner::_process`.
- This will involve calling `Com_Frame` or the equivalent entry point to drive the game engine.

## Reference Files
- [SConstruct](file:///home/elgan/dev/opm-godot/openmohaa/SConstruct) (Build config)
- [register_types.cpp](file:///home/elgan/dev/opm-godot/openmohaa/code/godot/register_types.cpp) (GDExtension Entry)
- [Main.gd](file:///home/elgan/dev/opm-godot/project/Main.gd) (Test script)
- [misc.h](file:///home/elgan/dev/opm-godot/openmohaa/code/fgame/misc.h) (Where FallingRock lives)
